# Comment lancer le projet
dotnet run

# config Keycloak
id du Realm = tp-realm
id du client = dotnetapp
RedirectUri  = http://localhost:3000/redirect

# Disclaimer
Le client secret est en clair dans le code car il s'agit d'un code dev pour un tp